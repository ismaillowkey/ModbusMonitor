<Window x:Class="ModbusMonitor.EditMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ModbusMonitor"
        Title="Edit Monitor" Height="300" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style TargetType="Button" x:Key="SaveButton">
            <Setter Property="Background" Value="#FFC107"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="CancelButton" BasedOn="{StaticResource SaveButton}">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>
        
        <Style TargetType="Button" x:Key="CloseButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="5">
                            <TextBlock Text="✕" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="8" BorderBrush="#DEE2E6" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FFC107">
                <Grid Margin="15,0">
                    <TextBlock Text="Edit Monitor" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    <Button Style="{StaticResource CloseButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="BtnCancel_Click"/>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20">
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Address" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtAddress" Height="35" Padding="10,0" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Data Type" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbDataType" Height="35" Padding="10,0" VerticalContentAlignment="Center" SelectionChanged="CmbDataType_SelectionChanged">
                            <ComboBoxItem Content="Int16 (16-bit)"/>
                            <ComboBoxItem Content="UInt16 (16-bit)"/>
                            <ComboBoxItem Content="Int32 (32-bit)"/>
                            <ComboBoxItem Content="UInt32 (32-bit)"/>
                            <ComboBoxItem Content="Float32 (32-bit)"/>
                            <ComboBoxItem Content="Int64 (64-bit)"/>
                            <ComboBoxItem Content="UInt64 (64-bit)"/>
                            <ComboBoxItem Content="Double (64-bit)"/>
                            <ComboBoxItem Content="String (1 char/word)"/>
                            <ComboBoxItem Content="String (2 chars/word)"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Byte Order (Endianness)" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbEndianness" Height="35" Padding="10,0" VerticalContentAlignment="Center">
                            <ComboBoxItem Content="ABCD (Big Endian)"/>
                            <ComboBoxItem Content="DCBA (Little Endian)"/>
                            <ComboBoxItem Content="BADC (Byte Swap)"/>
                            <ComboBoxItem Content="CDAB (Word Swap)"/>
                        </ComboBox>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock x:Name="lblLength" Text="Length (Words)" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtLength" Height="35" IsReadOnly="True" Background="#E9ECEF" Foreground="#495057" Padding="10,0" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#DEE2E6">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Button Content="Cancel" Style="{StaticResource CancelButton}" Margin="0,0,10,0" Click="BtnCancel_Click"/>
                    <Button Content="Save Changes" Style="{StaticResource SaveButton}" Click="BtnSave_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
